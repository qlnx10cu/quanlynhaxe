import React from "react";
import { connect } from "react-redux";

const CustomerInfo = (props) => {
    return <React.Fragment></React.Fragment>;
};

export default connect(null, null)(CustomerInfo);
